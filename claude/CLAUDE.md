@RTK.md
@CONTEXT.md

# Compact instructions

Preserve verbatim, do not paraphrase: exact file paths and line numbers touched, unresolved TODOs and next steps, exact error messages and stack traces, decisions made and their rationale, and current state of any in-progress long-running task (migration, build, refactor, multi-step plan) including the path of its plan file. Reference code by file:line instead of dropping snippets that may still be needed.
